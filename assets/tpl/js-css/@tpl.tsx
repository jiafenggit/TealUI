/**
 * @file __description
 * @author __author <__email>
 */
import { Control, VNode, bind, event } from "control";
import "./__fileName.scss";

/**
 * 表示一个__description。
 */
export class __FileName extends Control {

    /**
     * 当被子类重写时负责渲染当前控件。
     * @return 返回一个虚拟节点。
     */
    protected render() {
        return <div class="x-__filename"></div>;
    }

}

export default __FileName;
